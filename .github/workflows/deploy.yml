name: Build e Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Instalar dependências
        run: npm install
      - name: Build
        run: npm run build
      - name: Deploy (permarke.com.br)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 185.173.111.40
          username: u615659830.marialuizaemtr
          password: "_Permarke2026_"
          port: 21
          local-dir: ./dist/
          server-dir: /public_html/
